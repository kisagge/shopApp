'use client';

/**
 * 매장 화면이 깨졌을 때.
 *
 * **앱 뿌리의 오류 화면과 같은 것을 그린다.** 다만 이쪽은 `(shop)` 그룹 안에
 * 있어서 머리와 발 사이에 뜬다 — 화면 하나가 깨졌다고 돌아갈 길까지 사라지면
 * 사람은 주소를 지우는 수밖에 없다. 뿌리 쪽은 레이아웃 자체가 깨진 마지막
 * 자리라 그 껍데기를 믿을 수 없고, 그래서 맨몸으로 둔다.
 */
export { default } from '../error';
