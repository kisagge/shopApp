import { GridSkeleton } from '~/components/section-skeleton';

/**
 * 검색 결과를 기다리는 동안.
 *
 * **loading.tsx 가 있는 화면은 404 를 낼 수 없다.** 이 파일이 있으면 Next 가
 * 껍데기를 먼저 흘려보내는데, 그 순간 상태 코드가 200 으로 확정된다 — 그
 * 뒤에 notFound() 를 불러도 본문만 404 화면이고 코드는 200 인 **가짜 404**
 * 가 된다. 검색엔진은 그런 주소를 살아 있는 것으로 보고 계속 들고 있는다.
 *
 * 상품·카테고리·브랜드에도 붙였다가 그래서 뺐다. 없는 상품 주소가 전부
 * 200 으로 나가고 있었다.
 *
 * 검색은 조건이 무엇이든 화면이 열린다 — 결과가 없으면 "없다" 고 말하는
 * 것이지 없는 주소가 아니다. 그래서 여기만 남긴다.
 */
export default function Loading() {
  return <GridSkeleton />;
}
