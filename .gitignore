# 의존성
node_modules/
.pnpm-store/

# 빌드 산출물
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo
apps/web/next-env.d.ts

# 환경변수 (절대 커밋 금지)
.env
.env.*
!.env.example

# Capacitor 네이티브 프로젝트 — 생성물은 제외, 설정은 추적
apps/mobile/ios/App/Pods/
apps/mobile/ios/App/build/
apps/mobile/ios/App/App/public/
apps/mobile/android/.gradle/
apps/mobile/android/build/
apps/mobile/android/app/build/
apps/mobile/android/app/src/main/assets/public/
apps/mobile/android/local.properties
*.xcuserstate
xcuserdata/

# Prisma
packages/db/prisma/*.db
packages/db/prisma/*.db-journal

# 테스트
coverage/
playwright-report/
test-results/
.nyc_output/

# 디자인 캔버스 — 시드된 산출물(2.8MB). 아트보드 소스와 build.sh로 재생성됨
design/plain-commerce-design.html

# 에디터 · OS
.idea/
.vscode/
.DS_Store
*.swp

# 로그
*.log
npm-debug.log*
pnpm-debug.log*

# Prisma 생성물 (prisma generate 로 재생성)
packages/db/src/generated/
.vercel

# 에이전트 도구가 만드는 스킬 문서.
#
# Prisma CLI 가 `migrate dev` 같은 명령을 돌릴 때 패키지 안에 풀어 놓는다.
# 우리가 쓴 코드가 아니라 도구가 받아 온 참고 문서라, 저장소에 두면 기능
# 커밋에 90개씩 섞여 든다 — 실제로 한 번 그랬다. 필요하면 각자 기계에서
# 다시 받는다.
.agents/
.windsurf/
skills-lock.json
packages/*/.claude/

