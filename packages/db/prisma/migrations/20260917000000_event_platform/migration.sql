-- 어디서 들어온 방문인가 — 브라우저인가, 앱 웹뷰인가.
--
-- 지금까지 이벤트에 붙는 구분은 UA 에서 뽑은 기기 종류뿐이라 앱 웹뷰가
-- 모바일 브라우저와 같은 칸에 들어갔다. 앱은 켤 때마다 웹뷰를 차게 띄우는
-- 값이 더 붙는데, 그것이 모바일 웹 숫자에 섞여 둘 다 뿌옇게 보였다.
--
-- 이 컬럼이 생기기 전 행은 null 로 남는다. 채워 넣을 근거가 없다 —
-- 원본 User-Agent 를 저장하지 않으므로 지난 행에서 되짚을 방법이 없고,
-- 전부 'web' 으로 적으면 없는 사실을 지어내는 것이 된다.
--
-- 색인은 새로 걸지 않는다. 이 값을 쓰는 질의(실사용자 성능)는
-- name·receivedAt 으로 이미 좁힌 뒤에 묶기만 한다.
ALTER TABLE "event_logs" ADD COLUMN "platform" TEXT;
